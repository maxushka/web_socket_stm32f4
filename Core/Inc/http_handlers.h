#ifndef _HTTP_HANDLERS
#define _HTTP_HANDLERS

#include "main.h"
#include "net.h"
#include <string.h>

//char *GET_Handler(char *params);
//char *POST_Handler(char *url, char *json, struct webworker *web);

#endif
